"""
Chapter 3, Exercise 2: Greetings
"""
# Task: Start with the list you used in Exercise 1, but instead of just printing each person’s name, print a message to them. 
# The text of each message should be the same, but each message should be personalized with the person’s name.

friend_names=["Princess","Bayla","Jarryl","Rhianne","Renz","Raphne"]

message="Hi, "+friend_names[0]+", how are you? Let's see each other soon!"
print(message)
message="Hi, "+friend_names[1]+", how are you? Let's see each other soon!"
print(message)
message="Hi, "+friend_names[2]+", how are you? Let's see each other soon!"
print(message)
message="Hi, "+friend_names[3]+", how are you? Let's see each other soon!"
print(message)
message="Hi, "+friend_names[4]+", how are you? Let's see each other soon!"
print(message)
message="Hi, "+friend_names[5]+", how are you? Let's see each other soon!?"
print(message)

# A message is added to the variable along with the names of each person.
# Then, each personalized message is printed.
